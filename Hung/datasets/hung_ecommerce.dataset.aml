Dataset hung_ecommerce {
  label: 'Hung Ecommerce 4'
  data_source_name: 'demodb'
  owner: 'hung.nguyen@holistics.io'

  models: [
    order_master,
    products,
    categories,
    cities,
    users,
    order_details_test
  ]

  relationships: [
    relationship(order_master.user_id > users.id, true),
    relationship(order_master.product_id > products.id, true),
    relationship(products.category_id > categories.id, true),
    relationship(users.id > cities.id, true)
  ]
}