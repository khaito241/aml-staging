import '../base/order_items.model.aml' {orders_items}
import '../base/orders.model.aml' {orders}

Model order_master {
  type: 'query'
  label: 'Order Master'
  data_source_name: 'demodb'
  models: [order, orders_items]

  query: @sql
  
}