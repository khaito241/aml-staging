Dataset ecommerce_tai {
  label: "Ecomerce Tai (AML)"
  description: ""
	owner: 'tai.nguyen@holistics.io'
  data_source_name: "demodb"

  models: [
    users, 
    order_derived,
    merchants,
    categories,
    ecom_location, 
  ]

  relationships: [relationship(order_derived.user_id > users.id, true), relationship(users.city_id > ecom_location.city_id, false)]
} 