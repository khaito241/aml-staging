Dataset role_playing_dim {
  __engine__: 'aql'
  label: 'Role Playing Dim'
  description: ''
  data_source_name: 'demodb'
  models: [
    sales,
    dim_dates
  ]
  relationships: [
    relationship(sales.order_created_date > dim_dates.date, true)
    ,
    relationship(sales.delivered_date > dim_dates.date, false),
    relationship(sales.cancelled_date > dim_dates.date, false),
    relationship(sales.refunded_date > dim_dates.date, false)
  ]
  owner: 'khai.to@holistics.io'
}
